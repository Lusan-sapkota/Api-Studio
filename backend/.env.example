# =============================================================================
# API Studio - Environment Configuration
# =============================================================================
# 
# IMPORTANT: Choose your deployment mode first!
# 
# For LOCAL mode (single-user, no authentication):
#   Copy .env.local.example to .env
# 
# For HOSTED mode (multi-user with authentication):
#   Copy .env.hosted.example to .env
# 
# This file shows all available configuration options.
# =============================================================================

# =============================================================================
# CORE CONFIGURATION
# =============================================================================

# Application Mode (REQUIRED)
# "local"  = Single-user mode, no authentication required
# "hosted" = Multi-user mode with full authentication system
APP_MODE=local

# Database Configuration
DATABASE_URL=sqlite:///./api_studio.db

# Basic Security (REQUIRED)
SECRET_KEY=your-secret-key-here-change-in-production-min-32-chars
ALGORITHM=HS256

# Frontend URL for CORS (REQUIRED)
FRONTEND_URL=http://localhost:56173

# =============================================================================
# HOSTED MODE CONFIGURATION (Required when APP_MODE=hosted)
# =============================================================================

# JWT Configuration (REQUIRED for hosted mode)
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRY=86400

# SMTP Configuration (REQUIRED for hosted mode)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
SMTP_USE_TLS=true

# Bootstrap Configuration (REQUIRED for hosted mode initial setup)
ADMIN_BOOTSTRAP_TOKEN=your-secure-bootstrap-token-change-this

# =============================================================================
# LOCAL MODE CONFIGURATION (Optional when APP_MODE=local)
# =============================================================================

# Optional admin credentials for local development
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# =============================================================================
# SECURITY SETTINGS (Optional - defaults provided)
# =============================================================================

# OTP and Authentication Settings
OTP_EXPIRY=600                    # OTP expiry in seconds (10 minutes)
MAX_LOGIN_ATTEMPTS=5              # Max failed login attempts before lockout
LOGIN_LOCKOUT_DURATION=900        # Lockout duration in seconds (15 minutes)

# 2FA Settings
APP_NAME=API Studio               # Name shown in authenticator apps

# =============================================================================
# SERVER SETTINGS (for start.py script)
# =============================================================================

HOST=0.0.0.0
PORT=58123
RELOAD=true                       # Set to false in production

# Development Settings
WEBSOCKET_DEBUG=true              # Set to false in production

# =============================================================================
# QUICK START GUIDE
# =============================================================================
# 
# 1. Choose your mode:
#    - Local: Copy .env.local.example to .env
#    - Hosted: Copy .env.hosted.example to .env
# 
# 2. Update the configuration values in your .env file
# 
# 3. For hosted mode, ensure SMTP is properly configured
# 
# 4. Run the application:
#    python start.py
# 
# 5. For hosted mode, complete the bootstrap process to create your first admin
# 
# =============================================================================

