# =============================================================================
# API Studio - Local Mode Configuration
# =============================================================================
# This configuration is for single-user, local development without authentication
# Perfect for personal use, development, and testing

# Application Mode - MUST be "local" for single-user mode
APP_MODE=local

# Database Configuration
DATABASE_URL=sqlite:///./api_studio.db

# Basic Security (still needed for internal operations)
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256

# Frontend URL for CORS
FRONTEND_URL=http://localhost:56173

# Server Settings (for start.py script)
HOST=0.0.0.0
PORT=58123
RELOAD=true

# Optional: Create default admin user for local development
# These credentials will be used to create a local admin user automatically
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Development Settings
WEBSOCKET_DEBUG=true

# =============================================================================
# Notes for Local Mode:
# =============================================================================
# - No authentication required - direct access to all features
# - No email/SMTP configuration needed
# - No JWT tokens or 2FA
# - Perfect for personal API testing and development
# - All users have full access to all features
# =============================================================================